services:
  directus:
    image: directus/directus:10.12.1
    ports:
      - 8055:8055
    volumes:
      - $LOCAL_WORKSPACE_FOLDER/database:/directus/database
      - $LOCAL_WORKSPACE_FOLDER/uploads:/directus/uploads
      - $LOCAL_WORKSPACE_FOLDER/extensions:/directus/extensions
    environment:
      KEY: "replace-with-random-value"
      SECRET: "replace-with-random-value"
      ADMIN_EMAIL: "admin@example.com"
      ADMIN_PASSWORD: "d1r3ctu5"
      DB_CLIENT: "sqlite3"
      DB_FILENAME: "/directus/database/data.db"
      WEBSOCKETS_ENABLED: "true"
    networks:
      - cms_default

networks:
  cms_default:
    external: true
