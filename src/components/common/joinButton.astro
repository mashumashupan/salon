---
import button_bg from '~/assets/join_button.svg';
import { Image } from 'astro:assets';

const { text, url } = Astro.props;
---

<div class="h-max w-max">
    <a href={url}>
        <Image src={button_bg} alt="Join" />
        <span class="relative left-4 top-2">{text}</span>
    </a>
</div>
